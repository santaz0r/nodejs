<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous"></link>
    <title><%= title %></title>
</head>
<body>
    <% if (created) { %>
        <div class="alert alert-success">Note has been created</div>
        <% } %>  
    <div class="container">
        <h1><%= title %></h1>
        <form action="/" method="post" class="mb-2">
            <div class="form-group mb-2">
                <input type="text" class="form-control" name="title">
            </div>
            <button class="btn btn-primary" type="submit">Create</button>
        </form>
        <% if(notes.length) { %> 
        <div class="card">
            <ul class="list-group">
                <% for (let i = 0; i<notes.length; i++) { %> 
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <span data-type="inputSpan" data-id="<%= notes[i].id %>" ><%= notes[i].title %></span> 
                    <input data-type='inputField' class="change-input visually-hidden" data-id="<%= notes[i].id %>"  value="<%= notes[i].title %>"/> 
                    <div>
                        <button class="btn btn-success visually-hidden" data-id="<%= notes[i].id %>" data-title="<%= notes[i].title %>" type="submit" data-type="submit">Сохранить</button>
                        <button class="btn btn-danger visually-hidden " data-id="<%= notes[i].id %>" data-title="<%= notes[i].title %>" type="submit" data-type="cancel">Отмена</button>
                        <button class="btn btn-primary " data-id="<%= notes[i].id %>" data-title="<%= notes[i].title %>" type="submit" data-type="edit">Редактировать</button>
                        <button class="btn btn-danger" data-id="<%= notes[i].id %>" type="submit" data-type="remove">&times;</button>
                    </div>
                    
                </li>
                <% }  %> 
            </ul>
        </div>
        <% } else { %> 
            <p>No notes</p>
            <% } %> 
    </div>
    <script src="/app.js"></script>
</body>
</html>